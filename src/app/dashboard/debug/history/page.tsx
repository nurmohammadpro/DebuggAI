/**
 * Debug History Page
 *
 * View past debugging sessions with filter and re-run options.
 */

'use client';

import { DebugHistory } from '@/components/dashboard/debug/debug-history';

export default function DebugHistoryPage() {
  return <DebugHistory />;
}
